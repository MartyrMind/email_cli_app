# Email CLI Application

CLI приложение для отправки электронных писем с поддержкой Markdown, вложений и множественных получателей.

## Возможности

- 📧 Отправка писем через Gmail, Yandex, Outlook
- ✍️ Поддержка Markdown с live preview
  - Пишете в Markdown → получатель видит красивый HTML
  - Поддержка таблиц, списков, кода, цитат
  - Автоматическая конвертация в HTML для email-клиентов
- 📎 Прикрепление файлов
  - Автоматическое определение MIME типов
  - Поддержка Office документов (.docx, .xlsx, .pptx)
  - Поддержка старых форматов (.doc, .xls, .ppt)
  - PDF, изображения, архивы и другие
- 👥 Множественные получатели
- 🔔 Уведомления о статусе отправки
- ⚡ Асинхронная отправка

### Как работает Markdown в письмах?

Email-клиенты не поддерживают Markdown напрямую, поэтому приложение:

1. **Вы пишете** в Markdown (видите live preview в приложении)
2. **Приложение конвертирует** Markdown в HTML
3. **Отправляется две версии** письма:
   - Plain text (исходный Markdown) - для старых клиентов
   - HTML (красивый форматированный текст) - для современных клиентов
4. **Получатель видит** красиво отформатированное письмо с заголовками, списками, жирным текстом и т.д.

**Пример:**

**Вы пишете (Markdown):**
```markdown
# Важное уведомление
**Срочно!** Прочитайте до конца недели.

## Что нужно сделать:
- Проверить документы
- Подписать договор
- Отправить на email
```

**Получатель видит (HTML в email-клиенте):**
```
╔══════════════════════════════════════╗
║ Важное уведомление                   ║
╠══════════════════════════════════════╣
║                                      ║
║ Срочно! Прочитайте до конца недели.  ║
║                                      ║
║ Что нужно сделать:                   ║
║   • Проверить документы              ║
║   • Подписать договор                ║
║   • Отправить на email               ║
║                                      ║
╚══════════════════════════════════════╝
```
*С красивым форматированием, жирным текстом и структурой! ✨*

📝 **Смотрите больше примеров в файле `MARKDOWN_EXAMPLES.md`**

## Установка

```bash
# Установка зависимостей
poetry install

# Или через pip
pip install textual aiosmtplib markdown
```

## Настройка SMTP

### Шаг 1: Создайте конфигурационный файл

```bash
cp src/smtp_config.example.py src/smtp_config.py
```

### Шаг 2: Заполните авторизационные данные

Откройте `src/smtp_config.py` и заполните данные для нужных серверов:

#### Gmail
1. Включите двухфакторную аутентификацию
2. Создайте App Password: https://myaccount.google.com/apppasswords
3. Заполните `GMAIL_EMAIL` и `GMAIL_PASSWORD`

#### Yandex
1. Используйте свой email и пароль
2. Возможно потребуется разрешить доступ для внешних приложений
3. Заполните `YANDEX_EMAIL` и `YANDEX_PASSWORD`

#### Outlook/Office365
1. Используйте свой email и пароль
2. Убедитесь, что включена SMTP аутентификация
3. Заполните `OUTLOOK_EMAIL` и `OUTLOOK_PASSWORD`

### Шаг 3: Переключите режим

В `src/smtp_config.py` измените:
```python
TEST_MODE = False  # Для реальной отправки
```

⚠️ **Важно**: Не коммитьте `smtp_config.py` с реальными данными в git!

## Использование

```bash
# Запуск приложения
python src/app.py

# Логи будут писаться в:
# - Консоль (real-time)
# - email_sender.log (файл в корне проекта)
```

### Режимы работы

- **Тестовый режим** (`TEST_MODE = True`): Симулирует отправку писем без реального соединения с SMTP
- **Рабочий режим** (`TEST_MODE = False`): Реальная отправка через настроенные SMTP серверы

## Структура проекта

```
email_cli_app/
├── src/
│   ├── app.py                  # Основное приложение (UI)
│   ├── email_sender.py         # Логика отправки писем
│   ├── smtp_config.py          # Конфигурация (не коммитить!)
│   ├── smtp_config.example.py  # Шаблон конфигурации
│   └── styles.css              # Стили интерфейса
├── pyproject.toml
└── README.md
```

## Разработка

### Архитектура

- **app.py**: Textual UI, обработка событий, управление уведомлениями
- **email_sender.py**: Асинхронная отправка писем, SMTP логика
- **smtp_config.py**: Авторизационные данные (локально)

### Добавление нового SMTP сервера

1. В `smtp_config.py` добавьте новые переменные:
   ```python
   NEW_SERVER_EMAIL = ""
   NEW_SERVER_PASSWORD = ""
   ```

2. В `email_sender.py` обновите функцию `get_smtp_servers()`:
   ```python
   "NewServer": SMTPConfig(
       host="smtp.newserver.com",
       port=465,
       use_tls=True,
       email=NEW_SERVER_EMAIL,
       password=NEW_SERVER_PASSWORD
   )
   ```

3. В `app.py` добавьте опцию в Select виджет

## Отладка и логирование

### Просмотр логов

Все операции логируются в:
- **Консоль**: real-time вывод во время работы
- **Файл `email_sender.log`**: полная история отправок

```bash
# Просмотр последних логов
tail -f email_sender.log

# Поиск ошибок
grep ERROR email_sender.log

# Поиск по конкретному письму
grep "email_1" email_sender.log
```

### Уровни логирования

- `INFO`: Основные события (старт, отправка, успех)
- `WARNING`: Предупреждения (отсутствующие вложения)
- `ERROR`: Ошибки (SMTP ошибки, неверные данные)
- `DEBUG`: Детальная информация (для разработки)

### Типичные проблемы

#### 1. "SMTP credentials not configured"
**Причина**: Не заполнены данные в `smtp_config.py`  
**Решение**: Откройте `smtp_config.py` и заполните email/password

#### 2. "Unknown server: Gmail (smtp.gmail.com)"
**Причина**: Несоответствие названий серверов между UI и конфигурацией  
**Решение**: Используйте простые названия: "Gmail", "Yandex", "Outlook"  
**Проверка**: Смотрите в логе строку "Selected server: '...'"

#### 3. "Authentication failed" (Gmail)
**Причина**: Используется обычный пароль вместо App Password  
**Решение**: Создайте App Password: https://myaccount.google.com/apppasswords

#### 4. Письмо "успешно отправлено", но не пришло
**Причина**: Работает в тестовом режиме (`TEST_MODE = True`)  
**Решение**: Измените `TEST_MODE = False` в `smtp_config.py`

#### 5. "Attachment not found: example_document.pdf"
**Причина**: Попытка отправить несуществующий демо-файл  
**Решение**: Используйте кнопку "📎 Add Files" для выбора реальных файлов

#### 6. Connection timeout
**Причина**: Блокировка SMTP порта файрволом/антивирусом  
**Решение**: Проверьте доступ к портам 465/587

## Безопасность

- ✅ `smtp_config.py` добавлен в `.gitignore`
- ✅ Используются App Passwords вместо обычных паролей
- ✅ Конфиденциальные данные не хардкодятся в коде
- ✅ Логи не содержат паролей
- ⚠️ Не передавайте `smtp_config.py` третьим лицам

## Лицензия

MIT
